---
machine:
    python:
        version: 3.4.2

dependencies:
    override:
        - pip install -r requirements/development.txt

test:
    override:
        - flake8

deployment:
    production:
        branch: master
        commands:
            - sh scripts/deploy_to_heroku.sh circle
